Run the SESSION END protocol from CLAUDE.md:

1. Finalize today's `.claude/tasks/done/task_num-<slug>.md` log: outcome, what shipped,
   what's verified (with the command output you ran), open threads, and any bug + root cause.
2. Author a fresh `.claude/tasks/NextWork.md` for the next session: the next task (next item in
   gaps.md's remediation order), cold-start context, relevant files with `path:line`, gotchas, and
   what comes after.
3. Update `gaps.md` status for anything closed this session (flip the item's Status and adjust the
   severity counts in the executive summary).

Show me the `done/` log and the new NextWork.md before you finish.
